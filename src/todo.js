export default class ToDo {
    constuctor(project, title, description, dueDate) {3
        this.project = project;
        this.title = title;
        
        // add
            // description;
            // dueDate;
            // priority
            // notes
            // checklist
    }
}